"""Query services — structured read models for operator views.

Each module returns dataclasses or typed dicts that are screen-ready
but not tied to any specific rendering format (CLI text, HTML, JSON).
"""
