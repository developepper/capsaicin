# T02: Empty-implementation diagnostics

**Phase**: 2 — Empty-Run Diagnostics
**Type**: Implementation

## Goal

Make empty implementation outcomes explain themselves in human-gate and related
operator messaging.

## Scope

- improve `ticket run`, `loop`, and human-gate output for empty implementations
- when available, include a truncated agent result text snippet in the message
- when the run was blocked by permissions, say so explicitly and include a
  summary count of denied actions
- include the high-signal denied tool names in operator-facing output when that
  adds clarity
- ensure operator-facing wording distinguishes:
  - no changes made
  - blocked by permissions
  - other execution failures already handled elsewhere

## Non-goals

- no new `doctor` command
- no activity log changes

## Acceptance criteria

- empty implementation messages surface agent result text when it is useful
- permission-blocked runs explicitly mention denied actions rather than reading
  as plain empty implementations
- short remediation text such as "grant write permission" is preserved in the
  surfaced message when the agent already provided it
- loop stop messages and direct `ticket run` output are consistent

## Dependencies

- T01

## References

- `docs/README.md`
- `docs/cli.md`
- `docs/state-machine.md`
- `docs/adapters.md`
- `docs/tickets/epic-02-reliability-and-diagnostics/README.md`
- `docs/tickets/archive/epic-01-implementation-loop-mvp/T15.md`
- `docs/tickets/archive/epic-01-implementation-loop-mvp/T27.md`

## Implementation Notes

- Read the real agent result text from the captured permission-denied fixtures
  and preserve its remediation guidance when it is short and actionable.
- Keep default operator output concise. Surface the most important explanation
  first instead of dumping raw envelope fields.
- Reuse shared post-run handling where possible; do not duplicate formatting
  logic separately for `ticket run` and `loop` if they can share a helper.
- Preserve the existing human-gate model and state transitions. This ticket is
  about messaging and outcome explanation, not changing loop control.
- Do not add UI work here; constrain output changes to the CLI surfaces named in
  scope.
