# T17: Review result validation

**Phase**: 6 — Review Pipeline
**Type**: Implementation
**Status**: Complete

## Goal

Standalone validation of parsed ReviewResult per `adapters.md` rules.

## Scope

- `src/capsaicin/validation.py`
- `validate_review_result(result: ReviewResult, criteria_ids: list[str])
  -> ValidationResult`
- Rules:
  - `verdict:fail` requires >=1 blocking finding
  - `verdict:pass` cannot have blocking findings
  - `confidence:high` invalid with empty `files_examined`
  - `confidence:high` invalid with criteria provided but `criteria_checked` empty
  - `criteria_checked` entries must reference valid criterion IDs
  - `acceptance_criterion_id` on findings must reference valid criterion IDs
  - All top-level fields present
- `ValidationResult`: `is_valid: bool`, `violations: list[str]`
- Tests: one test per rule, plus happy-path valid result

## Non-goals

- No finding persistence
- No AC updates

## Acceptance criteria

- Each rule individually tested
- Valid results pass
- Each invalid case produces a specific violation description
- Validation is pure (no DB, no side effects)

## Dependencies

- T10

## Notes

T13 should import and call this function rather than duplicating validation
logic. Build T17 before or alongside T13.
