name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docs/meta check
        run: |
          test -f docs/overview.md
          test -f docs/README.md
          test -d docs/tickets

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install (editable)
        run: pip install -e ".[dev]"

      - name: Ruff format check
        run: ruff format --check src tests

      - name: CLI smoke test
        run: capsaicin --help

      - name: Tests
        run: pytest

      - name: Init smoke test
        run: |
          mkdir -p /tmp/capsaicin-smoke && cd /tmp/capsaicin-smoke
          git init
          capsaicin init --project smoke-test --repo /tmp/capsaicin-smoke
          test -f /tmp/capsaicin-smoke/.capsaicin/projects/smoke-test/capsaicin.db
          test -f /tmp/capsaicin-smoke/.capsaicin/projects/smoke-test/config.toml
          test -f /tmp/capsaicin-smoke/.capsaicin/projects/smoke-test/activity.log
